import React from "react";
import { BadgeCheck } from "lucide-react";

const VerifiedBadge = () => (
  <BadgeCheck
    size={16}
    className="fill-blue-500 text-white ml-1 inline-block shrink-0"
  />
);

export default VerifiedBadge;
